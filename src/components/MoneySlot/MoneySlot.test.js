import React from 'react';
import {shallow, mount} from 'enzyme';
import MoneySlot from './MoneySlot';

describe('MoneySlot', () => {
  it('should exist', () => {
    expect(MoneySlot).toBeDefined();
  })

})